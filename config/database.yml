default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  # Use environment variables for connection details
  host: <%= ENV.fetch("DATABASE_HOST") %>
  username: <%= ENV.fetch("POSTGRES_USERNAME") %>
  password: <%= ENV.fetch("POSTGRES_PASSWORD") %>

development:
  <<: *default
  database: todo_app_development # Default Rails naming convention

test:
  <<: *default
  database: todo_app_test # Default Rails naming convention

  # You'll configure production similarly, likely using different ENV vars
  # production:
  #   <<: *default
  #   database: todo_app_production
  #   username: postgres
  #   password: <%= ENV["POSTGRES_PASSWORD"] %>